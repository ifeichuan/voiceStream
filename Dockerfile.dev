FROM node:22-bookworm-slim

ENV COREPACK_ENABLE_DOWNLOAD_PROMPT=0

WORKDIR /app

RUN corepack enable

EXPOSE 1420

CMD ["sh", "-lc", "pnpm install --frozen-lockfile && pnpm dev --host 0.0.0.0 --port ${PORT_APP:-1420} --strictPort"]
